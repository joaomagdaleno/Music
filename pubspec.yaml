name: music_tag_editor
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  taglib_ffi_dart: ^0.0.3
  taglib_ffi_dart_libs: ^0.0.3
  file_picker: ^8.0.7
  http: ^1.2.1
  sqflite: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
