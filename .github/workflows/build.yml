name: Build, Test & Release

on:
  push:
    branches:
      - main
      - dev
      - beta
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
      - dev
      - beta

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

env:
  ACTIONS_STEP_DEBUG: true

jobs:
  quality_gate:
    name: Quality Gate
    permissions:
      contents: write
      pull-requests: write
    uses: ./.github/workflows/quality-gate.yml
    secrets: inherit

  build:
    name: Build & Package
    if: github.event_name == 'push'
    needs: quality_gate
    uses: ./.github/workflows/build-and-package.yml
    secrets: inherit

  pre_release:
    name: Pre-release
    if: github.event_name == 'push' && (github.ref == 'refs/heads/dev' || github.ref == 'refs/heads/beta')
    needs: build
    uses: ./.github/workflows/prerelease.yml
    secrets: inherit

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            artifacts/windows-installer/MusicTagEditorSetup-*.exe
            artifacts/android-apk/music_tag_editor-v*.apk
            artifacts/linux-release/music_tag_editor-linux-v*.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
